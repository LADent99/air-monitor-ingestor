apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "air-monitor-ingestor.fullname" . }}
  labels:
    {{- include "air-monitor-ingestor.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "air-monitor-ingestor.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "air-monitor-ingestor.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - secretRef:
                name: {{ include "air-monitor-ingestor.fullname" . }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
